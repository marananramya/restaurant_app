<!DOCTYPE html>
<html lang="en">
  <body>
    <%= render 'layouts/header'%>
    <div class="container">
      <div class="row">
      <% if @restaurants.present? %>
          <div class="panel">
            <div class="panel-heading">
              <h3>Restaurant Details</h3>
            </div>
            <div class="panel-body">
              <table class="table mbn tc-icon-bold">
                <tbody>
                  <tr>
                    <td>
                      Restaurant Name
                    </td>
                    <td>
                      City
                    </td>
                  </tr>
                  <% @restaurants.each do |r| %>
                  <tr>
                    <td>
                      <%= link_to r.name, restaurants_show_path(:id => r.id) %>
                    </td>
                    <td>
                      <%= r.city %>
                    </td>
                  </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
      <% else %>
        <div class="panel-heading">
          <h3 class="text-center">No Reviews Found</h3>
        </div>
      <% end %>
      </div>
    </div>
  </body>
</html>
